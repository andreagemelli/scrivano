[project]
name = "lfm-finetuning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "accelerate>=1.14.0",
    "datasets>=5.0.1",
    "pandas>=3.0.5",
    "torch>=2.14.0",
    "transformers>=5.16.1",
]
